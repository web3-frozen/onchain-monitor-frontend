name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  packages: write
  security-events: write
  id-token: write

jobs:
  ci:
    uses: web3-frozen/homelab-apps/.github/workflows/ci-container.yaml@main
    with:
      app-name: onchain-monitor-frontend
      image-name: onchain-monitor-frontend
      workload-dir: workloads/onchain-monitor
      node-version: "22"
    secrets: inherit
